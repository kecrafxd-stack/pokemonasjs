for(let i = 0; i<buttons_pokemon.length; i++){
    buttons_pokemon[i].addEventListener('click', () => {
        jugador_selection = obj_pokemon[buttons_pokemon[i].dataset.pokemon]
        console.log(jugador_selection)

        //Mostrar otros menu
        movimientos.classList.remove('desactive')
        consola.classList.remove('desactive')

        consola.children[0].textContent = "Seleccionaste a " + jugador_selection.name
        
        buttons_pokemon__container.remove()
    })
}

NOTAS

Ewe, tienes que hacer esto asi. Ambos eligiran su ataque al mismo tiempo, y despues el sistema decidira quien atacara primero segun su velocidad.
por ello, a la funcion deben entrar los dos objetos y sus dos selects, o en su defecto definirse todo dentro de la funcion